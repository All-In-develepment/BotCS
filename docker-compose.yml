version: "3"

services:
  bot_cs:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 8000:8000
    volumes:
      - .:/app
    networks:
      - networkBotCS

networks:
  networkBotCS:
    name: networkBotCS
